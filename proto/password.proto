syntax = "proto3";

package password;


option go_package = "./proto/password;password";


message ResetPassworsRequest {
    string email = 1;
}

message ResetPasswordResponse {
    string messagge = 1;
    string password = 2;
}

service PasswordService {
    rpc ResetPassword(ResetPassworsRequest) returns (ResetPasswordResponse);
}